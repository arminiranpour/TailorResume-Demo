from typing import Any, Dict


def score_fit(resume: Dict[str, Any], job: Dict[str, Any]) -> Dict[str, Any]:
    """Deterministic scoring entry point. Stub only."""
    raise NotImplementedError("score_fit is not implemented")
