Metadata-Version: 2.4
Name: tailorresume-ai-service
Version: 0.1.0
Summary: FastAPI service for deterministic resume-to-job pipeline (scaffolding only).
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.110.0
Requires-Dist: uvicorn[standard]>=0.29.0

# AI Service (FastAPI)

## Purpose
Deterministic pipeline service with provider abstraction (local or API).

## Run (Placeholder)
1. `python -m venv .venv && source .venv/bin/activate`
2. `pip install -e .`
3. `uvicorn app.main:app --reload`

## Environment
Copy `.env.example` to `.env` and adjust as needed.

## Schemas
Canonical JSON Schemas live in `shared/schemas/` at the repo root.
