# Frontend (Next.js)

## Purpose
Minimal UI + API routes for the deterministic resume-to-job pipeline.

## Run (Placeholder)
1. `pnpm install`
2. `pnpm dev`

## Environment
Copy `.env.example` to `.env.local` and set the AI service URL.
