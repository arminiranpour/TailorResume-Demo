# Shared

This folder contains artifacts shared conceptually between the frontend and AI service.

## Contents
- `schemas/`: Canonical JSON Schemas used for strict validation.
- `prompts/`: Prompt templates (added in later phases).
- `scoring/`: Deterministic scoring breakdown (docs only).
- `utils/`: Placeholder for shared utilities (kept empty for now).
